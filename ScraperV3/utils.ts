export const doShitWithRes = (data: (Map<string, object> | undefined)[]) => {
  //   if (data?.length) return;
  console.log("================================");
  console.log("Using cached data:");
  console.log("================================");

  data.map((x) => {});
};
